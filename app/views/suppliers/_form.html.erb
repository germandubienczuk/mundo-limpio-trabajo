

<%= form_for(@supplier, html: { class: "form-horizontal" }) do |f| %>

  <% if @supplier.errors.any? %>
    <div id="error_explanation">
      <div class="alert alert-danger">
        Error
      </div>
      <% @supplier.errors.full_messages.each do |msg| %>
        <div class="alert">* <%= msg %></div>
      <% end %>
    </div>
  <% end %>

  <fieldset>
  <legend><h1 class="text-center"><%= @legend %></h1></legend>
  <div class="form-group">
    <label class="col-md-4 control-label" for="name"></label>  
    <div class="col-md-4">
    <%= f.text_field :name, autofocus: true, class: "form-control input-md", placeholder: "Nombre" %>
      
    </div>
  </div>

  <!-- Button -->
  <div class="form-group">
    <label class="col-md-4 control-label" for="btn"></label>
    <div class="col-md-4">
      <br><br>
      <%= f.submit "Guardar", class: "btn btn-lg btn-primary  btn-block"%>
      <%= link_to 'Volver', suppliers_path ,:class=>"btn btn-lg btn-primary btn-block"%>
    </div>
  </div>

  <fieldset>
<% end %>